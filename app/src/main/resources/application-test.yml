spring:
  config:
    activate:
      on-profile: test
  security:
    user:
      name: internal-admin
      password: test-internal-admin-password
      roles: ADMIN

jobrunr:
  background-job-server:
    enabled: false
  dashboard:
    enabled: false

kartoush:
  jobs:
    cleanup:
      enabled: false
  email:
    delivery:
      provider: ${KARTOUSH_EMAIL_DELIVERY_PROVIDER:noop}
      enabled: ${KARTOUSH_EMAIL_DELIVERY_ENABLED:false}
