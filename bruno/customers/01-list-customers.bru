meta {
  name: List customers
  type: http
  seq: 1
}

get {
  url: {{host}}/api/customers
  body: none
  auth: none
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
